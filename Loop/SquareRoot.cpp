#include<iostream>
#include<cmath>
using namespace std;
int main(){
    //There is a built in Sq. Root Shortcut thats 'sqrt'
    //Its a Math Program, so we have to use a header '<cmath>'
    int a = 10;
    float s = sqrt(a);
    cout<<s;
}